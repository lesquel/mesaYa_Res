PGHOST='db.udikqvwaobzclmwggffh.supabase.co'
PGPORT='5432'
PGDATABASE='postgres'
PGUSER='postgres'
PGPASSWORD='miquel13536hwbsfsrt@'
PGSSLMODE='require'
PGCHANNELBINDING='disable'
PG_SSL_CERT_PATH=''
SUPABASE_URL='https://your-project-id.supabase.co'
SUPABASE_SERVICE_KEY='your-supabase-service-role-key'
SUPABASE_BUCKET='images'
APP_TITLE='MesaYa API'
APP_DESCRIPTION='MesaYa Reservation platform API'
APP_VERSION='1.0.0'
JWT_SECRET='change-this-super-long-secret-key'
JWT_EXPIRES_IN='1d'
AUTH_SALT_ROUNDS='10'
CORS_ENABLED='true'
CORS_ORIGINS='http://localhost:4200'
CORS_METHODS='GET,HEAD,PUT,PATCH,POST,DELETE,OPTIONS'
CORS_CREDENTIALS='true'
CORS_ALLOWED_HEADERS='Content-Type,Authorization'
CORS_EXPOSE_HEADERS=''
CORS_MAX_AGE='86400'

KAFKA_BROKER='localhost:29092'
KAFKA_CLIENT_ID='mesa-ya-res-service'
KAFKA_GROUP_ID='mesa-ya-res-group'

# Chatbot Microservice
CHATBOT_SERVICE_URL='http://localhost:8001'
CHATBOT_TIMEOUT_MS='30000'

# MCP Server for AI Tools (Sentiment Analysis, Vision, etc.)
MCP_SERVICE_URL='http://localhost:8002'
MCP_TIMEOUT_MS='30000'

# ============================================
# STRIPE PAYMENT GATEWAY CONFIGURATION
# ============================================
# Get these keys from: https://dashboard.stripe.com/developers
# Use test keys (sk_test_*, pk_test_*) for development

# Stripe Secret Key (Backend - NEVER expose to frontend)
STRIPE_SECRET_KEY='sk_test_your_secret_key_here'

# Stripe Publishable Key (Safe for frontend)
STRIPE_PUBLIC_KEY='pk_test_your_public_key_here'

# Stripe Webhook Secret (from Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET='whsec_your_webhook_secret_here'

# Default currency for payments (ISO 4217)
STRIPE_CURRENCY='usd'

# Use real Stripe in development (default: false - uses MockPaymentAdapter)
USE_REAL_STRIPE='false'

# ============================================
# B2B PARTNER WEBHOOKS
# ============================================
# Maximum retry attempts for failed webhooks (default: 3)
WEBHOOK_MAX_RETRIES='3'

# HTTP timeout for outgoing webhooks in ms (default: 5000)
WEBHOOK_TIMEOUT_MS='5000'

# Maximum age for incoming webhook timestamps in seconds (default: 300 = 5 minutes)
WEBHOOK_TIMESTAMP_TOLERANCE_SEC='300'

# Base URL for this service (used in webhook documentation)
BASE_URL='http://localhost:3000'

