import type { BaseLogMeta, BaseLogPayload } from '../../common';

// Re-export with Winston-specific naming for consistency
export type LogMeta = BaseLogMeta;
export type LogPayload = BaseLogPayload;
