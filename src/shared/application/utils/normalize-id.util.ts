/**
 * Normalizes an ID string by trimming whitespace.
 * @param value - The ID to normalize
 * @returns The trimmed ID string
 */
export function normalizeId(value: string): string {
  return value.trim();
}
